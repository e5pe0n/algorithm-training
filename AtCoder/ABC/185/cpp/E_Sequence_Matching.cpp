#include <bits/stdc++.h>
#define MAX_N 1000
#define MAX_M 1000
using namespace std;

int N, M;
int A[MAX_N], B[MAX_M];

void solve() {}

int main() {
  cin >> N >> M;
  for (int i = 0; i < N; ++i) {
    cin >> A[i];
  }
  for (int i = 0; i < M; ++i) {
    cin >> B[i];
  }
  solve();
}